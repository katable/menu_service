var should = require('chai').should();

describe('MongoDB is seeded properly', function() {
  describe('MongoDB is seeded properly', function() {
    it('2 should be equal to 2', function() {
      (2).should.eql(3);
    })
    
  })


})
